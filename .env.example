# Copy using npm run setup; it creates unique local passwords.
HOST=127.0.0.1
PORT=3000
DATABASE_PATH=./data/wasselha.sqlite
RESTAURANT_PASSWORD=
COURIER_PASSWORD=
# Set true when serving behind HTTPS. Never expose this development server directly.
COOKIE_SECURE=false

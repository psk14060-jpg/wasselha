{
  "name": "wasselha",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "وصّلها — منصة لمطاعم ومناديب توصيل مستقلين، بديلة لعمولة تطبيقات التوصيل",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "dev": "node --env-file-if-exists=.env --watch src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/server.mjs && node --check src/store.mjs && node --check public/app.js"
  }
}
